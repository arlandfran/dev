---
import { buildBreadcrumbListJsonLd } from "../lib/breadcrumb";

const jsonLd = buildBreadcrumbListJsonLd(Astro.url.pathname);
---

<script
  type="application/ld+json"
  is:inline
  set:html={JSON.stringify(jsonLd)}
/>
